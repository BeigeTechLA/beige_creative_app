// Re-export shim — canonical location is `lib/core/network/api_endpoints.dart`.
// Phase 5 will retire this path; until then both imports resolve to the same class.
export 'package:beige_creative_app/core/network/api_endpoints.dart';
